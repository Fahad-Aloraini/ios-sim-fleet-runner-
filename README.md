# ios-sim-fleet-runner-
Drop-in bash runner that executes Maestro flows across N iOS simulators over arbitrary rounds, preserving each simulator's app data between rounds. Includes a SwiftUI/SwiftData demo app.
